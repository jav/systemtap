#! stap -p4

probe kernel.function("rebalance_tick") {
  log ("rebalance_tick (" . string ($this_cpu) . ", " .
                            string ($this_rq) . ", " .
                            string ($idle) .
			 ")")
}
